This Python quiz game asks multiple-choice questions, checks user responses, calculates the score, and provides feedback based on performance.

This quiz game:

    Uses lists and dictionaries to store questions.
    Uses loops to iterate through questions.
    Takes user input and checks correctness.
    Keeps track of and displays the final score.
